# CloudBridge Tiering Advisor — Python dependencies
# Pinned for reproducible builds; update with `pip-compile` when upgrading.

# Web framework + ASGI server
fastapi==0.111.0
uvicorn[standard]==0.29.0

# PostgreSQL async driver
asyncpg==0.29.0

# ML / data
scikit-learn==1.4.2
numpy==1.26.4
pandas==2.2.2

# HTTP client (calls CloudBridge gateway)
httpx==0.27.0

# Data validation (pulled in by FastAPI, pinned for stability)
pydantic==2.7.1
